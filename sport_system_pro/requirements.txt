# 校园运动打卡与健康数据分析系统 v2.0 依赖
# Python 3.10+

# ── 核心依赖（必装）──
matplotlib>=3.7.0        # 数据可视化，嵌入 tkinter 图表

# ── 可选依赖（增强功能）──
openpyxl>=3.1.0          # Excel 导出（.xlsx 格式）；未安装时自动退回 CSV

# ── 开发/测试依赖 ──
pytest>=7.4.0            # 单元测试框架
pytest-cov>=4.1.0        # 测试覆盖率报告

# ── 标准库（无需安装，仅供说明）──
# tkinter   - GUI 框架（随 Python 附带）
# sqlite3   - 数据库（随 Python 附带）
# csv/json  - 数据读写（随 Python 附带）
# hashlib   - 密码哈希（随 Python 附带）
# logging   - 日志系统（随 Python 附带）
# pathlib   - 路径管理（随 Python 附带）
